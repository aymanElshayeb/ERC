<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Share files from the app's cache directory -->
    <cache-path name="cache" path="." />

    <!-- Share files from the app's external files directory -->
    <external-files-path name="external_files" path="." />

    <!-- Share files from the app's files directory -->
    <files-path name="files" path="." />

    <!-- Share files from a custom directory -->
    <external-path name="external" path="Download/" />

</paths>
